<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="#1FDB27" />

    <!-- Góc bo tròn -->
    <corners android:radius="16dp" />

    <!-- Viền (stroke) -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />
</shape>